# consolidate_user_memory(user_id) (fetch pending episodes, recalc importance, chunk for Qwen-Long, 
# resolve contradictions, write back semantic/procedural rows, mark episodes promoted), 
# decay_refresh(user_id) (exponential forgetting curve + pruning)