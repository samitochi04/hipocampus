# GET /memory/conflicts (lists semantic facts flagged is_conflicted=true), 
# GET /memory/export (dumps a user's episodic/semantic/procedural rows as JSON), 
# PATCH /memory/facts/{id} (lets the user manually confirm or edit a semantic fact)