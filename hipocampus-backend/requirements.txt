# hipocampus-backend/requirements.txt
# Pin major versions; let patch versions float for security fixes.

# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.0        # [standard] pulls in uvloop + httptools for production speed

# --- Database ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.29.0                  # Async PostgreSQL driver used by SQLAlchemy
alembic==1.13.3
pgvector==0.3.6                  # SQLAlchemy column type + psycopg helpers for vector columns

# --- Redis ---
redis[asyncio]==5.1.0             # redis-py with asyncio support

# --- Task queue ---
celery==5.4.0
kombu==5.3.4                     # Celery's messaging library (pinned to avoid version drift)

# --- Auth / crypto ---
pyjwt==2.9.0
argon2-cffi==23.1.0              # Argon2id hashing for login keys

# --- HTTP client (Qwen API calls) ---
httpx==0.27.0

# --- Validation ---
pydantic==2.9.2
pydantic-settings==2.5.2         # Loads Settings from env vars / .env files

# --- Document generation ---
fpdf2==2.8.1                     # Pure-Python PDF generation; no system deps required

# --- Document upload / text extraction ---
python-multipart==0.0.32
PyMuPDF==1.24.10                 # PDF text extraction — imports as `import fitz`

# --- Web search (MCP tool execution) ---
# duckduckgo_search removed — _run_search now uses httpx directly
# (html.duckduckgo.com endpoint) to avoid the primp rate-limit issue.

# --- Voice TTS ---
dashscope>=1.21.0                # Official DashScope SDK — used for qwen3-tts-flash
                                 # TTS via the WebSocket SpeechSynthesizer API.
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.0                    # Already listed above; httpx doubles as the test HTTP client
fakeredis==2.26.0                # In-memory Redis for tests — no server needed